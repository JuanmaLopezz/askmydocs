from langchain_core.documents import Document


def embed_and_store(chunks: list[Document], collection_name: str) -> int:
    """Generate embeddings and persist in ChromaDB. Returns chunk count."""
    # TODO: Fase A — implement embeddings + ChromaDB persistence
    raise NotImplementedError("Fase A pendiente")
