from langchain_core.callbacks import BaseCallbackHandler


def get_langfuse_callback() -> BaseCallbackHandler | None:
    """Return LangFuse callback if credentials are configured, else None."""
    # TODO: Fase D — implement LangFuse integration
    return None
